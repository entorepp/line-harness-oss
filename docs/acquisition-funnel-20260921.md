# Unified acquisition funnel — 2026-09-21

Owner requested separate counts for request receipt, rendered page, interaction, and saved submission. Initial scope is the public Flat Travel site and Accessible Japan form 9ab583b2-e42e-4ca2-bcb9-13a3c59f5477. Existing survey implementations outside this form are not instrumented by this change.

## Definitions
- Every non-prefetch document GET is recorded before rendering, including failed rendering. This is an HTTP receipt, not proof of a human click on Accessible Japan. Existing tagged links work without replacement.
- Optional /go/accessible-japan on flat-travel.com and /go/aj-form on liffform-studio.pages.dev record a separate redirect receipt before the landing page. Partner must adopt these URLs for this extra indicator. Existing partner links were not edited.
- page_visible requires a foreground document, the main form/site container, and two animation frames; form fields must exist. Receipt without visible acknowledgement is unconfirmed display, not proven abandonment.
- Trusted interaction events record no entered values. Each form field/step records at most once per page. Link clicks record each click with an idempotency UUID; retrying delivery does not multiply counts.
- submit_success is server-only, after the existing intake confirms save. A 200 with an unsuccessful body cannot convert. No customer submission is generated by deployment.
- Random first-party HttpOnly af_visit cookie lasts 30 minutes; reloads are separate request rows in the same visit. UTMs normalize Accessible Japan aliases. Missing UTM is unattributed, never inferred as direct.
- Reports cohort by visits started within the selected UTC interval, showing subsequent events received by query time. First-party clicks, UTM requests, and visit-stage totals are separate units. Tests marked af_test=1 or aj_test=1 inherit test status and are excluded.

## Storage and access
New acquisition_visits/pages/events/clicks tables in existing line-crm D1. Forms uses existing FORM_TRAFFIC_DB; public site adds ACQUISITION_DB. No IP, raw referrer, user-agent, customer identity or answer values. Existing optional Google/Meta consent behavior remains unchanged. Only fixed public paths, campaign tokens, fixed field identifiers and anonymous IDs are retained.
GET /__acquisition/report uses the existing protected AJ report bearer token. /acquisition-report.html accepts the existing report fragment key; the existing shared report links to it. Unauthenticated API access must return 401. The site cannot serve reports. No new account, credential, background job or provider API.

## Verification and release
Canonical implementation: packages/acquisition-funnel/core.js in line-harness-oss. Generate _worker.js with scripts/build-acquisition-funnel.mjs; public-site copy differs only by the TypeScript nocheck header. Real SQLite tests cover receipt/visit linkage, redirects, test exclusion, client-forged success rejection, deduplication, origin checks and report authorization. Collector VM verifies foreground/two-frame acknowledgement and trusted events.
Release gates: existing Forms production branch + deploy-forms-studio-production.sh; public site managed release v84.259.96-acquisition-funnel + deploy-production.mjs. Apply only migration 020; set Pages production FUNNEL_V1_ENABLED=true before guarded deployment. Do not deploy the separate Flat Harness Worker.
Rollback: disable FUNNEL_V1_ENABLED on the affected runtime and redeploy through its guard. Preserve D1 tables/history. No deletion or credential rotation needed.

## Acceptance boundary
Prepared and locally tested; production deployment and live receipt readback are not yet verified. Update ops evidence after release. Past missing events cannot be reconstructed. Other questionnaire forms require separate adapter registration; no claim of universal form coverage.
